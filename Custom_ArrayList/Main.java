/**
 * Main
 */
public class Main {

    public static void main(String[] args) {
        // ArrayList<Integer> a=new ArrayList<>();
        // a.add(new Integer(1));
        // Integer a1=new Integer(1);
        
        // System.out.println(a.lastIndexOf(a1));

        // ArrayList<Student> s=new ArrayList<>();
        // s.add(new Student(1));
        // Student s1=new Student(1);

        // System.out.println(a.lastIndexOf(s1));

        // ArrayList<Integer> a=new ArrayList<>();
        // a.add(1);

        // ArrayList<Long> b=new ArrayList<>();
        // b.add((long)100000);

        // long c=1;
        // b.add(c);

        // CustomArrayList<Student> a1 = new CustomArrayList<>();
        // a1.add(new Student(1));
        // System.out.println(a1.lastIndexOf(s));
        // a.add(1);
        // a.remove(0);
        // a.add(1);
        // a.add(2);
        // for (int i=0; i<a.size(); i++) {
        //     System.out.println(a.get(i));
        // }
    }
}

class Student{
    int name;
    Student(int name){
        this.name=name;
    }
}